let titre = "Hello  super aprem";
titre = 18;

const selfie = {
    titre: 'Wahoo '
};

class Selfie  {
    titre = 'hello';
}

function afficher(selfie) {
    console.info(selfie.titre);
}
afficher(selfie);
afficher(18);